Executar a API: mvn -q -DskipTests package && java -cp target/classes EstoqueServer
Abrir index.html no navegador ou servir via: python -m http.server 5500
A URL base pode ser alterada definindo window.API_BASE antes de carregar app.js.
